apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "webmethods-microservice-runtime.fullname" . }}-test-connection"
  labels:
    {{- include "webmethods-microservice-runtime.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "webmethods-microservice-runtime.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
